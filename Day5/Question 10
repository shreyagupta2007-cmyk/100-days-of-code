#include <stdio.h>
int main() 
{
    int sec, hour, min, rem_sec;
    printf("Enter time in seconds: ");
    scanf("%d", &sec);
    hour=sec/3600;
    min=(sec%3600)/60;
    rem_sec=sec%60;
    printf("%02d:%02d:%02d\n", hour, min, rem_sec);
    return 0;
}
