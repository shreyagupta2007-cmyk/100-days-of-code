#include <stdio.h>
int main()
{
    int num,original,n,rev=0;
    printf("Enter a number: ");
    scanf("%d", &num);
    original=num;
    while(num>0)
    {
        n=num%10;
        rev=rev*10+n;
        num=num/10;
    }
    printf("Reverse of %d: %d", original, rev);
    return 0;
}
