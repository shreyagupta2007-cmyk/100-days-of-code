#include <stdio.h>
int main()
{
    int n,p=1;
    printf("Enter a number: ");
    scanf("%d", &n);
    for(int i=2;i<=n;i+=2)
    {
        p=p*i;
    }
    printf("Product of even no. upto %d: %d", n, p);
    return 0;
}
